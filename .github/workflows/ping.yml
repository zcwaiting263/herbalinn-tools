name: Keep Alive
on:
  schedule:
    - cron: '*/10 * * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping workbench
        run: curl -s -o /dev/null -w "%{http_code}" https://tools.herbalinn.cn/
